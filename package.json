{
  "name": "agent-kanban",
  "version": "1.0.0",
  "description": "Multi-agent monitoring dashboard with Kanban view",
  "scripts": {
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:frontend\"",
    "dev:server": "cd server && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "install:all": "npm install && cd server && npm install && cd ../frontend && npm install && cd ../reporter && npm install && cd ../hooks && npm install",
    "build": "cd server && npm run build && cd ../frontend && npm run build",
    "install-hooks": "cd hooks && node install.js",
    "uninstall-hooks": "cd hooks && node install.js uninstall"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
